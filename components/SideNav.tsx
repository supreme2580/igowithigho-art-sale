const SideNav = () => {
    return (
        <nav className="min-h-screen fixed w-64 border-r border-ash z-20 bg-white transform -translate-x-full transition duration-200 ease-in-out sidebar md:hidden"></nav>
    )
}

export default SideNav